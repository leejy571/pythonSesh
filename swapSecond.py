a = 50
b = 100

print ("The original values are " + str(a) + " and " + str(b))

temp = a
a = b
b = temp

print ("The swapped values are %d and %d" % (a, b))
print ("The swapped values are " + str(a) + " and " + str(b))
